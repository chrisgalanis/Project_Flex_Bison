public class A
{   
  double _A_a_2;
   boolean _a;
   String AB;
   public int _a_b;
   private int _a_1;
   

   int x;

    x = (5 *  3) - 4 ;
    x = (5 + 4) / 4 ;
    x =  5 + (4 * 4);
    x = 5 + (2 - 4) ;
    x = 5 * (2 + 4) ;
    x = 6 * ( 2 / 4) ;

    String a, b, c;

    a = "Hello123$" ;
    b = "This is a valid str1ng";
    c = "this is valid; \'valid\' too";

    A a = new A();
    a.x;
  


    public void func_1 () 
    {
        
        for (int i = 0; i < 10; i++)
        {

            break;

        }


        // IF Statement
        if (true)
        {

        }else if ( x && y || z){

        }else{

        }

        if (x){

        }



        // switch
       switch (x)
        {
            case 1:
                x = 15;

                break;
            case 2:
                break;

            default: 
            
        }

        //X a.func1();
        out.print("Hello world 123 @$@");
        out.println("Hello world!"); 

        out.print("aba", k , y, h);
        out.println("aba", k , x , z);

    }

   



}

